# <<AGENT_NAME>>

<<MODEL_VERSION_OR_PROVIDER>>

## Default role

<<DEFAULT_ROLE>>

(One of the roles defined in `../../protocols/roles.md`, e.g. "TL",
"Worker", "Auditor", "Lead". Or "Generalist" if the agent floats.)

## When the operator invokes me

Boot via `../../README.md` (the router); then read `inbox/` for any
pending messages. If the operator hasn't specified a role for this
session, use the default above.

## Operating mode

See `operating-mode.md` (or nothing if I use the project defaults).

## Personal style

<<ONE_OR_TWO_LINES>>

(Optional: anything specific about how this agent prefers to work
that other agents should know — e.g. "responds in short paragraphs,
prefers diff-style proposals", "asks for review before final
commits, never auto-commits even when instructed".)

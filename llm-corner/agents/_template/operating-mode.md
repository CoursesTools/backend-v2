# Operating Mode

Per-agent deviations from the project default protocols. Leave this
file as-is (or delete it) if I follow the defaults.

## Deviations from project defaults

<<DEVIATIONS>>

(Examples:
- "I respond to direct messages in inbox within ONE session;
  delayed replies trigger a re-ping."
- "Before committing, I always show a diff summary to the operator,
  even if they pre-approved the change."
- "I default to spawning a verification subagent on changes touching
  contract C2.")

## Tools I prefer to use

<<TOOLS>>

(Optional: which tools are my go-to. e.g. "Prefer `gh pr view` over
manual git commands"; "Use `rg` not `grep`".)

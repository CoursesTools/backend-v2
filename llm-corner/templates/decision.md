# DEC-NNN — <decision title, stated as the outcome>

Status: PROPOSED | ACCEPTED | SUPERSEDED by DEC-MMM
Date: YYYY-MM-DD
Author: <agent> (note operator involvement: blessed / requested / informed)

## Context

<The forces: what problem, what constraints, what evidence. Enough
that a future agent re-deciding this starts from the same facts.>

## Options considered

1. <option> — rejected: <why>
2. **CHOSEN: <option>** — <why it wins>

## Consequences

<What this commits us to; costs accepted; what would trigger
revisiting (link the metric/event). When revisited, write a new DEC
and mark this one SUPERSEDED — never edit history.>

---
After writing: add a row (newest first) to `../decisions/INDEX.md`.
Write a DEC when: a contract changes, a vendor is chosen/dropped, an
architecture shape is picked over a viable alternative, or an operator
instinct is overridden with reasons.
